{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: the main window may call the app's IPC commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
