<div class="page-head">
	<h1>Chat</h1>
	<p>An always-on AI tutor that speaks at your level.</p>
</div>

<div class="card">
	<p>
		This is <strong>Pillar 3</strong> (spec §2.3) and lands in <strong>Phase 2</strong>. The data
		model and engine boundary already exist in <code>glossa-conversation</code> — scenarios,
		turns, and a side-channel for corrections — so building it is filling in
		<code>ConversationEngine</code>, not redesigning anything.
	</p>
	<p class="muted">
		The same engine also powers the Pillar 5 AI fallback for human matching (Phase 4).
	</p>
</div>
